#к сожалению, получилось только для целых чисел. при вводе "," сбивается процесс перевода str(данные в list) в int(для суммирования)
text=input()
a=(len(text))
b=0
while b<a:
    b=b+1
c=list((text[0:b]))
#тут мы привратили одно число в массив чисел


result = list(map(int, c))
#Тут функция map применяет функцию int к каждому элементу объекта "c"

d=sum(result)
print(d)
